spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/fluxnexadb
    driver-class-name: org.postgresql.Driver
    hikari:
      username: albert
      password: albert
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
